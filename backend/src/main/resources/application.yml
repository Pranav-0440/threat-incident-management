spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.iqmubvaknlwogmyqcfbp.supabase.co:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${PORT:8080}

jwt:
  secret: YTJkM2Y1ZjctOGI5Ni00ZjEyLWFhMzMtNWQ2YjRhZGVmZjcxLXRocmVhdC1pbmNpZGVudC1tZ210LXNlY3JldC1rZXktMjAyNA==
  expiration: 86400000

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}

logging:
  level:
    com.threatmgmt: DEBUG
    org.springframework.security: DEBUG
